/*global define*/
define([
	'underscore',
	'backbone'
], function (_, Backbone) {
	'use strict';

	var StoryReaderModel = Backbone.Model.extend({
		defaults: {
			
		},
	});
	return StoryReaderModel;
});
